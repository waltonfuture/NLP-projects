## Using FOFE adapters on BERT

### There are FOFELayerWindows added to the BERT encoder to help the pretrained model better understand tokens' relationship.

#### run the scripts:

bash run_glue_adapter.bash

